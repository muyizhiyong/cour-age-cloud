server:
  port: 8702

eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8700/eureka
  instance:
    hostname: localhost

spring:
  application:
    name: eureka-consumer
